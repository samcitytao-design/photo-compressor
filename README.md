# 图像压缩工具

一个纯前端图片压缩工具。图片只在浏览器本地处理，不会上传到服务器。

## 功能

- 支持拖拽或点击上传多张图片
- 支持 WebP、JPEG、PNG 输出格式
- 支持质量和缩放比例调整
- 支持原图与压缩后预览
- 支持单张下载和批量 ZIP 下载
- 上传后不会自动压缩，点击下载时才开始本地处理
- 支持自定义命名规则、前后缀文本和起始编号

## 使用

直接打开 `index.html` 即可使用。上传图片后先调整格式、质量、缩放和命名规则，再点击下载。

部署到 GitHub Pages 时，将站点来源设置为 `main` 分支根目录。
